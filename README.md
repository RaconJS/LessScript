Javascript-like programming language for writing code shortly and quickly

In many other programming languages it's hard to stay dry when writing functions

e.g. javascript
	



reduces need to name arguments


e.g. sum all elements in a list:

Javascript:

`let sum=a=>a.reduce((s,v)=>s+v);`

LessScript:

`sum:\#.>\#+#;`

side by side translation comparison
`let sum = array => array . reduce ((sum,value) => sum + value);`
`    sum :       \  #     . >                   \  #   + #     ;`


